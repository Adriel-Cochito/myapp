export function soma(v1, v2) {
    console.log('SOMA Executada');
    return v1 + v2;
}

const soma(v1, v2) {
    console.log('SOMA Executada');
    return v1 + v2;
}

const  soma(v1, v2) => {
    console.log('SOMA Executada');
    return v1 + v2;
}

// Variáveis:
let a = 1;
const b = 2;
var c = 3;

// const x = soma(2, 3);
// console.log(x);
